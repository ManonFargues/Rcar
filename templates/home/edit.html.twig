{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        <div class="section">
            <div class="row">
                <div class="col s12 m12">
                    <h1>Modifier votre annonce : {{ car.model }}</h1>
                    {% include 'home/form.html.twig' %}
                    <button type="submit" class="btn btn-outline-primary my-4">Sauvegarder</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}