{% extends "base.html.twig" %}

{% block body %}

<div class="container">

    <!-- Heading Row -->
    <div class="row align-items-center my-5">
        <div class="col-lg-7">
            <img class="img-fluid rounded mb-4 mb-lg-0" src="http://placehold.it/900x400" alt="">
        </div>
        <!-- /.col-lg-8 -->
        <div class="col-lg-5">
            <h1 class="font-weight-light">Location de voiture : choisissez votre véhicule</h1>
            <p>Chez RCar, nous vous offrons la possibilité de louer une voiture adaptée à votre budget ainsi qu'à vos besoins. Vous avez aussi la possibilité de proposer votre propre voiture à la location.
                Nos voitures à louer sont regroupées par catégories. La location de voiture avec RCar est simple et économique. </p>
            <a class="btn btn-primary" href="{{ path("add") }}">Mettre une voiture en location</a>
            <a class="btn btn-primary" href="{{ path("search_car") }}">Rechercher</a>
        </div>
        <!-- /.col-md-4 -->
    </div>
    <!-- /.row -->

    <!-- Call to Action Well -->
    <div class="card text-white bg-secondary my-5 py-4 text-center">
        <div class="card-body">
            <p class="text-white m-0">This call to action card is a great place to showcase some important information
                or display a clever tagline!</p>
        </div>
    </div>

    <!-- Content Row -->


    <div class="row">
        <!--
        <div>
            <ul>
                {% for car in cars %}
                    <li><h3><a href="{{ path('show', {'id' : car.id}) }}">{{ car.model }}</a> </h3></li>
                {% endfor %}
            </ul>
        </div>
        -->
        {% for car in cars %}
        <div class="col-md-4 mb-5">
            <div class="card h-100">
                <div class="card-body">
                    <h2 class="card-title">{{ car.model }}</h2>
                    <p class="card-text">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Rem magni quas ex
                        numquam, maxime minus quam molestias corporis quod, ea minima accusamus.</p>
                    <p>Prix de la location : {{ car.price }} €</p>
                </div>
                <div class="card-footer">
                    <a href="{{ path('show', {'id' : car.id}) }}" class="btn btn-primary btn-sm">Détails</a>
                    <a href="{{ path('edit', {'id' : car.id}) }}" class="btn btn-warning btn-sm">Modifier</a>
                    <a href="{{ path('delete', {'id' : car.id}) }}" class="btn btn-danger btn-sm">Supprimer</a>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
    <!-- /.row -->

</div>

{% endblock %}