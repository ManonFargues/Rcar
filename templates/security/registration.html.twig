{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="section">
            <div class="row">
                <div class="col s12 m12 my-4">
                    <div>
                        <h1 class="h3 mb-3 font-weight-normal">Inscription</h1>
                    </div>
                    {{ form(form) }}
                    <div>
                        <p>Déjà un compte? <a href="{{ path('app_login') }}">Connectez-vous</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}